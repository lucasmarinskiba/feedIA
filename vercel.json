{
  "version": 2,
  "buildCommand": "echo 'START BUILD' && npm run build && echo 'TS DONE' && ls -la dist-static/ && mkdir -p public && echo 'MKDIR DONE' && cp -r dist-static/* public/ && echo 'COPY DONE' && ls -la public/ && cp public/index.html public/404.html && echo 'BUILD SUCCESS'"
}
