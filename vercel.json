{
  "version": 2,
  "buildCommand": "npm run build && mkdir -p public && cp -r dist-static/* public/",
  "rewrites": [
    {
      "source": "/api/(.*)",
      "destination": "/api"
    },
    {
      "source": "/((?!.*\\.\\w+$|^/public|^/api).*)",
      "destination": "/index.html"
    }
  ]
}
